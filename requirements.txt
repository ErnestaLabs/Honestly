# Core API & validation
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6

# Caching & coordination
redis>=5.0.0

# HTTP client
requests>=2.31.0

# Image processing
Pillow>=10.0.0

# Data science (gentrification radar, syndicate map)
pandas>=2.0.0
matplotlib>=3.7.0
darts>=2.0.0

# PDF & document parsing
pypdf>=3.0.0
unstructured>=0.10.0

# LLM integrations
langchain-openai>=0.1.0
langchain-core>=0.1.0

# Postgres / PostGIS
psycopg2-binary>=2.9.0

# OSM / Overpass API
overpy

# SSH (deploy tooling)
paramiko>=3.0.0

# Playwright-based browsing (canary, QA testing)
playwright>=1.40.0

# Mapping
geopandas>=0.14.0
shapely>=2.0.0
